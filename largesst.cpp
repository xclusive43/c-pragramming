//using function find the largest among three no,

#include<stdio.h>
#include<string.h>
#include<conio.h>
int main()
{ int largest();
getch();

} largest()
{int largest, a, b,c;
a=2; b=4; c=1;
if (a>b || a>c)
   {
      printf("largest %d", a);
   }
 else
  if(b>c & b>a)
  {
      printf("largest is %d", b);
  } 
  else 
  {
      printf("largest is %d", c); 
  }
  return 0;
  }
